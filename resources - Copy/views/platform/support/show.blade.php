@extends('layouts.platform',['title'=>'Support Conversation'])
@section('content')
<div class="mx-auto max-w-6xl space-y-6"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><a href="{{route('platform.support.index')}}" class="text-xs font-bold text-slate-500 hover:text-yellow-600">← Back to inbox</a><h1 class="mt-2 text-2xl font-black text-slate-900">{{$contactMessage->subject}}</h1><p class="mt-1 text-sm text-slate-500">Conversation with {{$contactMessage->name}}</p></div><form method="POST" action="{{route('platform.support.status',$contactMessage)}}">@csrf @method('PATCH')<button class="rounded-xl {{$contactMessage->status==='closed'?'bg-yellow-400 text-slate-900':'bg-slate-900 text-white'}} px-5 py-3 text-xs font-black">{{$contactMessage->status==='closed'?'Reopen conversation':'Close conversation'}}</button></form></div>
@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{{session('status')}}</div>@endif
@if($errors->any())<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">{{$errors->first()}}</div>@endif
<div class="grid items-start gap-6 lg:grid-cols-[minmax(0,1fr)_340px]"><section class="space-y-4"><article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-start gap-4"><span class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-yellow-400 font-black text-slate-900">{{strtoupper(substr($contactMessage->name,0,1))}}</span><div class="min-w-0 flex-1"><div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><b class="text-sm text-slate-900">{{$contactMessage->name}}</b><a href="mailto:{{$contactMessage->email}}" class="block text-xs text-blue-600 hover:underline">{{$contactMessage->email}}</a></div><span class="text-[10px] text-slate-400">{{$contactMessage->created_at->format('d M Y, H:i')}}</span></div><div class="mt-5 whitespace-pre-line text-sm leading-7 text-slate-700">{{$contactMessage->message}}</div><span class="mt-5 inline-flex rounded-full bg-slate-100 px-3 py-1 text-[9px] font-black uppercase text-slate-600">{{$contactMessage->type}}</span></div></div></article>
@foreach($contactMessage->replies as $reply)<article class="ml-5 rounded-2xl border border-yellow-200 bg-yellow-50/60 p-6 shadow-sm sm:ml-12"><div class="flex items-start justify-between gap-3"><div><b class="text-sm text-slate-900">Edlink Support · {{$reply->administrator?->name??'Platform administrator'}}</b><p class="mt-1 text-xs font-semibold text-slate-500">{{$reply->subject}}</p></div><span class="rounded-full px-2 py-1 text-[8px] font-black uppercase {{$reply->delivery_status==='sent'?'bg-emerald-100 text-emerald-700':($reply->delivery_status==='failed'?'bg-rose-100 text-rose-700':'bg-slate-200 text-slate-600')}}">{{$reply->delivery_status}}</span></div><div class="mt-4 whitespace-pre-line text-sm leading-7 text-slate-700">{{$reply->message}}</div><p class="mt-4 text-[10px] text-slate-400">{{$reply->sent_at?->format('d M Y, H:i')??$reply->created_at->format('d M Y, H:i')}}</p></article>@endforeach</section>
<aside class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:sticky lg:top-24"><h2 class="font-black text-slate-900">Reply by email</h2><p class="mt-1 text-xs text-slate-400">Your reply will be emailed to {{$contactMessage->email}} and saved here.</p>@if($contactMessage->status==='closed')<div class="mt-5 rounded-xl bg-slate-100 p-4 text-xs text-slate-600">Reopen this conversation before replying.</div>@else<form method="POST" action="{{route('platform.support.reply',$contactMessage)}}" class="mt-5 space-y-4">@csrf<label class="block text-xs font-bold text-slate-600">Subject<input name="subject" value="{{old('subject','Re: '.$contactMessage->subject)}}" class="mt-2 w-full rounded-xl border-slate-200 text-sm">@error('subject')<span class="mt-1 block text-[10px] text-rose-600">{{$message}}</span>@enderror</label><label class="block text-xs font-bold text-slate-600">Message<textarea name="message" rows="10" placeholder="Write a helpful response…" class="mt-2 w-full rounded-xl border-slate-200 text-sm leading-6">{{old('message')}}</textarea>@error('message')<span class="mt-1 block text-[10px] text-rose-600">{{$message}}</span>@enderror</label><button class="flex w-full items-center justify-center gap-2 rounded-xl bg-yellow-400 px-5 py-3 text-sm font-black text-slate-900 hover:bg-slate-900 hover:text-white">Send email reply</button><p class="text-center text-[9px] leading-4 text-slate-400">Delivery uses the mail service configured for Edlink.</p></form>@endif</aside></div></div>
@endsection