<div><div class="mb-8"><h1 class="text-2xl font-bold">Payroll</h1><p class="mt-1 text-sm text-slate-500">Pay active staff salaries from the school cash pool.</p></div>@if(session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-3 text-emerald-700">{{session('status')}}</div>@endif@if(session('error'))<div class="mb-4 rounded-xl bg-rose-50 p-3 text-rose-700">{{session('error')}}</div>@endif<label class="mb-5 block max-w-xs text-sm font-semibold">Payroll period<input wire:model="period" type="month" class="mt-1 w-full rounded-xl border-slate-200"></label><div class="overflow-hidden rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-400"><tr><th class="p-4">Staff</th><th class="p-4">Title</th><th class="p-4">Salary</th><th></th></tr></thead><tbody class="divide-y">@foreach($staff as $member)<tr><td class="p-4 font-semibold">{{$member->name}}</td><td class="p-4">{{$member->job_title}}</td><td class="p-4 font-mono">UGX {{number_format($member->base_salary)}}</td><td class="p-4 text-right">@if($payingId===$member->id)<button wire:click="confirm" class="font-bold text-rose-600">Confirm pay</button><button wire:click="$set('payingId',null)" class="ml-2">Cancel</button>@else<button wire:click="pay({{$member->id}})" class="rounded-lg bg-yellow-400 px-3 py-1.5 font-bold">Pay</button>@endif</td></tr>@endforeach</tbody></table></div></div>
