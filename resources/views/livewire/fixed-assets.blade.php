<div class="space-y-6">
@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif
@error('asset')<div class="rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $message }}</div>@enderror
<div class="rounded-3xl bg-slate-900 p-6 text-white"><div class="float-right flex gap-2"><a href="{{ route('accounting.exports',['report'=>'fixed-asset-register','format'=>'pdf']) }}" class="rounded-lg bg-red-500/20 px-3 py-2 text-xs font-bold">Register PDF</a><a href="{{ route('accounting.exports',['report'=>'fixed-asset-register','format'=>'xlsx']) }}" class="rounded-lg bg-emerald-500/20 px-3 py-2 text-xs font-bold">Register Excel</a><a href="{{ route('accounting.exports',['report'=>'depreciation-schedule','format'=>'xlsx']) }}" class="rounded-lg bg-blue-500/20 px-3 py-2 text-xs font-bold">Schedule Excel</a></div><p class="text-[10px] font-black uppercase tracking-[.2em] text-yellow-400">Accounting control ledger</p><h1 class="mt-1 text-2xl font-black">Fixed Asset Management</h1><p class="mt-2 max-w-3xl text-sm text-slate-300">Register, authorize, assign and depreciate school assets with every financial movement posted through the general ledger.</p><div class="mt-5 flex gap-2">@foreach(['register'=>'Asset Register','categories'=>'Categories & Rules','depreciation'=>'Depreciation'] as $key=>$label)<button wire:click="setTab('{{ $key }}')" class="rounded-xl px-4 py-2 text-xs font-bold {{ $tab===$key?'bg-yellow-400 text-slate-900':'bg-white/10' }}">{{ $label }}</button>@endforeach</div></div>
@if($tab==='register')
<form wire:submit="saveAsset" class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-3"><h2 class="md:col-span-3 text-lg font-black">Register an asset</h2>
@foreach([['assetTag','Asset tag'],['name','Asset name'],['serialNumber','Serial number'],['location','Location'],['acquisitionDate','Acquisition date','date'],['inServiceDate','In-service date','date'],['cost','Acquisition cost','number'],['residualValue','Residual value','number']] as $f)<label class="text-xs font-bold text-slate-600">{{ $f[1] }}<input type="{{ $f[2]??'text' }}" step="0.01" wire:model="{{ $f[0] }}" class="mt-1 w-full rounded-xl border-slate-300 text-sm"></label>@endforeach
<label class="text-xs font-bold">Category<select wire:model="categoryId" class="mt-1 w-full rounded-xl border-slate-300 text-sm"><option value="">Select category</option>@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->code }} · {{ $c->name }}</option>@endforeach</select></label><label class="text-xs font-bold">Settlement<select wire:model.live="settlementType" class="mt-1 w-full rounded-xl border-slate-300 text-sm"><option value="immediate">Paid immediately</option><option value="credit">Supplier credit</option></select></label>@if($settlementType==='immediate')<label class="text-xs font-bold">Payment account<select wire:model="financialAccountId" class="mt-1 w-full rounded-xl border-slate-300 text-sm">@foreach($financialAccounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label>@endif<label class="text-xs font-bold">Custodian<select wire:model="custodianId" class="mt-1 w-full rounded-xl border-slate-300 text-sm"><option value="">Unassigned</option>@foreach($staff as $u)<option value="{{ $u->id }}">{{ $u->name }}</option>@endforeach</select></label><label class="md:col-span-2 text-xs font-bold">Description<textarea wire:model="description" class="mt-1 w-full rounded-xl border-slate-300 text-sm"></textarea></label><div class="md:col-span-3">@error('*')<p class="mb-2 text-xs text-red-600">{{ $message }}</p>@enderror<button class="rounded-xl bg-yellow-400 px-6 py-3 text-sm font-black">Register and submit acquisition</button></div></form>
<div class="overflow-x-auto rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th>Tag / Asset</th><th>Category</th><th>Custodian</th><th>Cost</th><th>Accumulated depreciation</th><th>Carrying value</th><th>Status</th><th></th></tr></thead><tbody>@foreach($assets as $asset)<tr class="border-t"><td class="py-3"><b>{{ $asset->asset_tag }}</b><br><span class="text-slate-500">{{ $asset->name }}</span></td><td>{{ $asset->category->name }}</td><td>{{ $asset->custodian?->name??'Unassigned' }}</td><td>{{ number_format($asset->cost,2) }}</td><td>{{ number_format($asset->accumulatedDepreciation(),2) }}</td><td class="font-bold">{{ number_format($asset->carryingValue(),2) }}</td><td>{{ str($asset->status)->headline() }}</td><td>@if($asset->status==='awaiting_approval')<button wire:click="approveAsset({{ $asset->id }})" class="text-xs font-black text-emerald-700">Approve acquisition</button>@elseif($asset->status==='active')<button wire:click="depreciate({{ $asset->id }})" class="text-xs font-black text-blue-700">Run depreciation</button>@endif</td></tr>@endforeach</tbody></table><div class="mt-4">{{ $assets->links() }}</div></div>
@elseif($tab==='categories')
<form wire:submit="saveCategory" class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-3"><h2 class="md:col-span-3 text-lg font-black">Asset categories and ledger rules</h2><label class="text-xs font-bold">Code<input wire:model="categoryCode" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="text-xs font-bold">Name<input wire:model="categoryName" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="text-xs font-bold">Useful life (months)<input type="number" wire:model="categoryLife" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="text-xs font-bold">Method<select wire:model.live="categoryMethod" class="mt-1 w-full rounded-xl border-slate-300"><option value="none">Non-depreciable</option><option value="straight_line">Straight line</option><option value="reducing_balance">Reducing balance</option></select></label>@if($categoryMethod==='reducing_balance')<label class="text-xs font-bold">Annual rate %<input type="number" step="0.0001" wire:model="categoryRate" class="mt-1 w-full rounded-xl border-slate-300"></label>@endif<label class="text-xs font-bold">Asset cost account<select wire:model="assetAccountId" class="mt-1 w-full rounded-xl border-slate-300"><option value="">Select</option>@foreach($assetAccounts as $a)<option value="{{ $a->id }}">{{ $a->code }} · {{ $a->name }}</option>@endforeach</select></label><label class="text-xs font-bold">Accumulated depreciation<select wire:model="accumulatedAccountId" class="mt-1 w-full rounded-xl border-slate-300"><option value="">Select</option>@foreach($assetAccounts as $a)<option value="{{ $a->id }}">{{ $a->code }} · {{ $a->name }}</option>@endforeach</select></label><label class="text-xs font-bold">Depreciation expense<select wire:model="expenseAccountId" class="mt-1 w-full rounded-xl border-slate-300"><option value="">Select</option>@foreach($expenseAccounts as $a)<option value="{{ $a->id }}">{{ $a->code }} · {{ $a->name }}</option>@endforeach</select></label><div class="md:col-span-3"><button class="rounded-xl bg-yellow-400 px-6 py-3 text-sm font-black">Save category</button></div></form>
<div class="grid gap-3 md:grid-cols-2">@foreach($categories as $c)<div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><b>{{ $c->code }} · {{ $c->name }}</b><p class="mt-2 text-xs text-slate-500">{{ str($c->depreciation_method)->headline() }} · {{ $c->useful_life_months }} months</p><p class="mt-3 text-xs">Dr {{ $c->expenseAccount->code }} / Cr {{ $c->accumulatedAccount->code }}</p></div>@endforeach</div>
@else
<div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><label class="text-xs font-bold">Depreciation period ending<input type="date" wire:model="depreciationDate" class="ml-3 rounded-xl border-slate-300"></label><h2 class="mt-6 font-black">Awaiting independent approval</h2><table class="mt-3 min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th>Asset</th><th>Period</th><th>Opening</th><th>Depreciation</th><th>Closing</th><th></th></tr></thead><tbody>@forelse($pendingDepreciations as $d)<tr class="border-t"><td class="py-3">{{ $d->asset->asset_tag }} · {{ $d->asset->name }}</td><td>{{ $d->period_ending->format('d M Y') }}</td><td>{{ number_format($d->opening_carrying_value,2) }}</td><td>{{ number_format($d->depreciation_amount,2) }}</td><td>{{ number_format($d->closing_carrying_value,2) }}</td><td><button wire:click="approveDepreciation({{ $d->id }})" class="font-black text-emerald-700">Approve & post</button></td></tr>@empty<tr><td colspan="6" class="py-8 text-center text-slate-400">No depreciation journals awaiting approval.</td></tr>@endforelse</tbody></table></div>
@endif</div>
