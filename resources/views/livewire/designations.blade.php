<div class="mx-auto max-w-7xl space-y-6"><div><h1 class="text-2xl font-bold">Staff designations & access</h1><p class="mt-1 text-sm text-slate-500">Create a designation once, select its module rights, then assign it to staff. Everyone on the same designation receives the same access.</p></div>@if(session('error'))<div class="rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-800">{{session('error')}}</div>@endif@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800">{{session('status')}}</div>@endif@if($errors->any())<div class="rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-800">@foreach($errors->all() as $error)<div>{{ $error }}</div>@endforeach</div>@endif<div class="grid gap-6 lg:grid-cols-3"><form wire:submit="save" class="rounded-2xl border bg-white p-5 shadow-sm"><h2 class="font-bold">{{ $editingId ? 'Edit designation' : 'New designation' }}</h2><label class="mt-4 block text-sm font-medium">Name<input wire:model="name" class="mt-1 w-full rounded-xl border-slate-200" placeholder="e.g. Senior Teacher"></label><label class="mt-3 block text-sm font-medium">Description<textarea wire:model="description" rows="2" class="mt-1 w-full rounded-xl border-slate-200" placeholder="Optional responsibility summary"></textarea></label><p class="mt-4 text-sm font-bold">Access rights</p><div class="mt-2 space-y-3">@foreach($accessGroups as $module=>$group)<div class="rounded-xl border p-3"><p class="text-xs font-bold uppercase tracking-wide text-slate-500">{{ $group['label'] }}</p><div class="mt-2 grid gap-2">@foreach($group['rights'] as $key=>$label)<label class="flex items-center gap-2 text-sm"><input wire:model="permissions" type="checkbox" value="{{ $key }}">{{ $label }}</label>@endforeach</div></div>@endforeach</div><div class="mt-4 flex gap-2"><button class="rounded-xl bg-yellow-400 px-4 py-2 text-sm font-bold">{{ $editingId ? 'Save changes' : 'Create designation' }}</button>@if($editingId)<button type="button" wire:click="resetForm" class="rounded-xl border px-4 py-2 text-sm font-bold">Cancel</button>@endif</div></form><section class="overflow-hidden rounded-2xl border bg-white shadow-sm lg:col-span-2"><table class="w-full text-sm"><thead class="bg-slate-50 text-left text-slate-500"><tr><th class="p-4">Designation</th><th class="p-4">Access rights</th><th class="p-4">Staff</th><th class="p-4"></th></tr></thead><tbody class="divide-y">@forelse($designations as $designation)<tr><td class="p-4 font-bold">{{ $designation->name }}<span class="mt-1 block text-xs font-normal text-slate-400">{{ $designation->description }}</span></td><td class="p-4 text-xs text-slate-600">{{ collect($designation->permissions)->map(function($item) use ($accessGroups) { foreach($accessGroups as $group) if(isset($group['rights'][$item])) return $group['rights'][$item]; return $item; })->join(', ') ?: 'No access rights selected' }}</td><td class="p-4">{{ $designation->users_count }}</td><td class="p-4 text-right whitespace-nowrap"><button wire:click="edit({{ $designation->id }})" class="text-xs font-bold text-yellow-700">Edit</button><button wire:click="delete({{ $designation->id }})" wire:confirm="Remove this designation?" class="ml-3 text-xs font-bold text-rose-600">Delete</button></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">No designations have been created.</td></tr>@endforelse</tbody></table></section></div></div>
