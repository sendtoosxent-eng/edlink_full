<div><div class="mb-8"><h1 class="text-2xl font-bold">Subjects & teacher assignment</h1><p class="text-slate-500">All offerings are assigned to {{ $term ? $term->name.', '.$term->year : 'an open term' }}.</p></div>@if(session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-3 text-emerald-700">{{session('status')}}</div>@endif@if(session('error'))<div class="mb-4 rounded-xl bg-rose-50 p-3 text-rose-700">{{session('error')}}</div>@endif<div class="grid gap-6 lg:grid-cols-2"><form wire:submit="addSubject" class="rounded-2xl border bg-white p-6"><h2 class="font-bold">Add subject</h2><input wire:model="name" placeholder="Subject name" class="mt-4 w-full rounded-xl border-slate-200"><input wire:model="code" placeholder="Code (optional)" class="mt-3 w-full rounded-xl border-slate-200"><button class="mt-4 rounded-xl bg-yellow-400 px-5 py-2 font-bold">Save subject</button></form><form wire:submit="assign" class="rounded-2xl border bg-white p-6"><h2 class="font-bold">Assign to class and teacher</h2><select wire:model="subjectId" class="mt-4 w-full rounded-xl border-slate-200"><option value="">Subject</option>@foreach($subjects as $subject)<option value="{{$subject->id}}">{{$subject->name}}</option>@endforeach</select><select wire:model="classId" class="mt-3 w-full rounded-xl border-slate-200"><option value="">Class</option>@foreach($classes as $class)<option value="{{$class->id}}">{{$class->name}}</option>@endforeach</select><select wire:model="teacherId" class="mt-3 w-full rounded-xl border-slate-200"><option value="">Teacher</option>@foreach($teachers as $teacher)<option value="{{$teacher->id}}">{{$teacher->name}}</option>@endforeach</select><button class="mt-4 rounded-xl bg-slate-900 px-5 py-2 font-bold text-white">Assign for term</button></form></div><div class="mt-6 rounded-2xl border bg-white p-6"><h2 class="font-bold">School subjects</h2><div class="mt-4 flex flex-wrap gap-2">@forelse($subjects as $subject)<span class="rounded-full bg-slate-100 px-3 py-1 text-sm">{{$subject->name}} {{$subject->code ? '· '.$subject->code : ''}}</span>@empty<span class="text-slate-400">No subjects created yet.</span>@endforelse</div></div></div>
