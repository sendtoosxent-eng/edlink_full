<div class="space-y-6"><div><p class="text-sm font-bold uppercase tracking-wide text-yellow-700">{{ auth()->user()->designation?->name ?? ucfirst(auth()->user()->role) }}</p><h1 class="mt-1 text-2xl font-bold">Welcome, {{ auth()->user()->name }}</h1><p class="mt-1 text-slate-500">{{ $school->name }}{{ $term ? ' · '.$term->name.', '.$term->year : '' }}</p></div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($permissions as $permission)@if(isset($modules[$permission]))<a wire:navigate href="{{ route($modules[$permission][1]) }}" class="rounded-2xl border bg-white p-5 shadow-sm transition hover:border-yellow-400 hover:bg-yellow-50"><h2 class="font-bold">{{ $modules[$permission][0] }}</h2><p class="mt-1 text-sm text-slate-500">Open your permitted {{ strtolower($modules[$permission][0]) }} workspace.</p></a>@endif@empty<div class="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-900">No modules have been assigned. Contact the school administrator.</div>@endforelse</div><div class="grid gap-5 lg:grid-cols-2"><section class="rounded-2xl border bg-white p-5"><h2 class="font-bold">Today’s attendance</h2><p class="mt-2 text-3xl font-bold text-yellow-700">{{ $attendanceToday }}</p><p class="text-sm text-slate-500">attendance records captured today</p></section><section class="rounded-2xl border bg-white p-5"><h2 class="font-bold">Upcoming events</h2><div class="mt-3 space-y-2">@forelse($events as $event)<div class="rounded-lg bg-slate-50 p-3 text-sm"><b>{{ $event->title }}</b><span class="block text-slate-500">{{ $event->event_date->format('d M Y') }}</span></div>@empty<p class="text-sm text-slate-500">No upcoming events.</p>@endforelse</div></section></div></div>
